import 'package:flutter/material.dart';

class NilUI extends StatelessWidget {
  const NilUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
